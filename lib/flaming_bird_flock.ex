defmodule FlamingBirdFlock do
  @moduledoc """
  FlamingBirdFlock keeps the contexts that define your domain
  and business logic.

  Contexts are also responsible for managing your data, regardless
  if it comes from the database, an external API or others.

  Generated via `mix phx.new flaming_bird_flock --binary-id`.
  """
end
